"""Developer tooling for the World Cup 2026 project (not part of the engine).

Currently holds `depgraph` — a pure-stdlib import grapher that draws the
backend's connected structure (see `python run.py graph`)."""
